import styled from "styled-components";

const Content = styled.div`
  display: flex;
  margin-left: 5rem;
  justify-content: center;
  flex-direction: column;
`;

const Header = styled.h1`
  width: 100%;
  display: flex;
`

const Body = styled.div`
  display: flex;
`

export const S = { Content, Header, Body}